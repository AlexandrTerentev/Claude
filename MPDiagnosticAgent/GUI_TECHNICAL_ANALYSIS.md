# 🔬 GUI: Технический Анализ с Кнопками Применения

## Новая вкладка: "🔬 Technical Analysis"

### Что добавлено:

✅ **Отдельная вкладка** для технического анализа
✅ **Выбор .bin файла** или автоматический анализ последнего
✅ **Кнопка "🔬 Анализировать Лог"** - запуск анализа
✅ **Автоматический парсинг рекомендаций** из ответа Claude AI
✅ **Кнопки "✓ Применить"** для каждой рекомендации
✅ **Автоприменение параметров** к дрону в один клик

---

## Как использовать

### 1. Скачайте лог
- Перейдите на вкладку **"📥 Download Logs"**
- Подключитесь к дрону (если ещё не подключены)
- Скачайте нужный лог

### 2. Запустите анализ
- Перейдите на вкладку **"🔬 Technical Analysis"**
- (Опционально) Нажмите **"📁 Выбрать файл"** для конкретного лога
- Нажмите **"🔬 Анализировать Лог"**
- Подождите 10-30 секунд

### 3. Примените рекомендации
- Агент выдаст технический анализ
- Под результатами появятся **карточки "⚡ Быстрые действия"**
- Каждая карточка содержит:
  - Название параметра (например, `ATC_RAT_RLL_P`)
  - Изменение значения (например, `0.06 → 0.135`)
  - Кнопку **"✓ Применить"**

### 4. Один клик = изменение параметров
- Нажмите **"✓ Применить"**
- Подтвердите в диалоге
- Параметры автоматически запишутся в дрон
- Получите уведомление **"Можете тестировать дрон"**

---

## Пример workflow

```
1. Скачали лог → log_4_20251207.bin (3 MB)

2. Нажали "🔬 Анализировать Лог"
   ⏳ Анализирую лог... (15 секунд)

3. Результаты:
   ✅ Вибрации: ОТЛИЧНЫЕ (7-10 m/s²)
   ⚠️ PID: Занижены в 2-3 раза
   ✅ Моторы: Баланс ±1.2%
   ❌ GPS: 0 спутников

4. Быстрые действия:
   📦 Изменить ATC_RAT_RLL_P
       0.06 → 0.135
       [✓ Применить]

   📦 Изменить ATC_RAT_RLL_I
       0.06 → 0.090
       [✓ Применить]

   📦 Изменить ATC_RAT_RLL_D
       0.002 → 0.005
       [✓ Применить]

5. Нажали "✓ Применить" на всех трёх
   ✅ Параметры обновлены!
   ✅ Можете тестировать дрон

6. Полетели тестировать новые настройки 🚁
```

---

## Технические детали

### Парсинг рекомендаций

Агент автоматически находит в тексте Claude AI:
- Паттерн: `Parameter P/I/D: old → new`
- Пример: `Roll P: 0.06 → 0.135`

Затем маппит на реальные параметры ArduPilot:
```python
'Roll' → 'ATC_RAT_RLL_P'
'Pitch' → 'ATC_RAT_PIT_P'
'Yaw' → 'ATC_RAT_YAW_P'
```

### Применение параметров

```python
# Для каждого параметра:
self.agent.mav.set_parameter(
    param_name='ATC_RAT_RLL_P',
    param_value=0.135,
    param_type=9  # MAV_PARAM_TYPE_REAL32
)
```

### Безопасность

- ✅ Требуется подключение к дрону
- ✅ Диалог подтверждения перед применением
- ✅ Отображение старого и нового значения
- ✅ Уведомление о результате (успех/ошибка)

---

## Расширенные возможности

### Автоматическое применение ВСЕХ рекомендаций

Можно добавить кнопку "Применить всё":

```python
def apply_all_recommendations(self):
    """Apply all recommendations at once"""
    if not self.connected:
        messagebox.showwarning("Не подключен", "...")
        return

    for rec in self.current_recommendations:
        for param, value in rec['params'].items():
            self.agent.mav.set_parameter(param, value, 9)

    messagebox.showinfo("Успех", "Все рекомендации применены!")
```

### История изменений

Сохранение истории применённых параметров:

```python
self.param_history = []

# При применении:
self.param_history.append({
    'timestamp': datetime.now(),
    'param': param_name,
    'old_value': old_value,
    'new_value': new_value
})
```

### Откат изменений

Кнопка "↶ Откатить":

```python
def rollback_last_change(self):
    """Rollback last parameter change"""
    if not self.param_history:
        return

    last = self.param_history.pop()
    self.agent.mav.set_parameter(
        last['param'],
        last['old_value'],
        9
    )
```

---

## Скриншоты (описание)

### Вкладка "🔬 Technical Analysis"

```
┌─────────────────────────────────────────┐
│ 🔬 Технический Анализ Логов             │
├─────────────────────────────────────────┤
│ Глубокий анализ: вибрации, PID,        │
│ моторы, GPS.                            │
│                                         │
│ Выбранный лог: log_4_20251207.bin       │
│ [📁 Выбрать файл]                       │
│                                         │
│      [🔬 Анализировать Лог]             │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🔬 ТЕХНИЧЕСКИЙ АНАЛИЗ               │ │
│ │                                     │ │
│ │ Вибрации: ✅ ОТЛИЧНЫЕ               │ │
│ │ PID: ⚠️ Занижены в 2-3 раза        │ │
│ │ ...                                 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ⚡ Быстрые действия:                    │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Изменить ATC_RAT_RLL_P              │ │
│ │ 0.06 → 0.135        [✓ Применить]  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Изменить ATC_RAT_RLL_I              │ │
│ │ 0.06 → 0.090        [✓ Применить]  │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

---

## Требования

- Подключение к дрону (MAVLink)
- Права на запись параметров
- Скачанные .bin логи (>100KB)
- Claude CLI для AI анализа

---

## FAQ

**Q: Что делать если кнопки "Применить" не появились?**
A: Убедитесь что Claude AI выдал рекомендации в формате `Parameter: old → new`

**Q: Можно ли применить изменения без подключения к дрону?**
A: Нет, для применения требуется активное MAVLink соединение

**Q: Что делать если параметр не применился?**
A: Проверьте логи, возможно параметр защищён или дрон не поддерживает его

**Q: Как откатить изменения?**
A: Запишите старые значения вручную или используйте "Reset to Defaults" в Mission Planner

---

**Версия:** MPDiagnosticAgent v6.1
**Дата:** 2025-12-08
