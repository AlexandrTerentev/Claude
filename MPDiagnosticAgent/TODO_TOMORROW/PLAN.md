# ПЛАН НА ЗАВТРА - MPDiagnosticAgent v6.0

**Дата:** 2025-12-08
**Версия:** 6.0 → 6.1 (AI Integration)

---

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА

**AI Assistant НЕ ОТВЕЧАЕТ УМНО!**

Сейчас используется примитивный pattern matching вместо реального AI.

### Что не работает:
- ❌ AI отвечает шаблонными фразами
- ❌ Нет интеграции с Claude API
- ❌ Нет анализа через нейросеть
- ❌ Нет связи с GitHub dataset
- ❌ Нет умных решений проблем

---

## ✅ ЧТО СДЕЛАНО СЕГОДНЯ

### 1. Unified Application (main.py)
- ✅ Создано единое приложение с 4 вкладками
- ✅ AI Assistant чат (но без реального AI!)
- ✅ Auto-Fix с кнопками Apply
- ✅ Download Logs через MAVLink
- ✅ Settings отображение

### 2. Pattern Matching (НЕ ДОСТАТОЧНО!)
- ✅ Распознавание простых вопросов
- ✅ Базовые ответы по шаблонам
- ⚠️ НО: Это не настоящий AI!

### 3. Claude API Integration (НАЧАТО)
- ✅ Добавлен метод `ask_claude_api()` в unified_agent.py
- ✅ Интеграция с Claude CLI
- ⚠️ НО: Требует установки и тестирования

### 4. Команда agent_ran
- ✅ Глобальная команда работает
- ✅ Запуск из любого места

---

## 🎯 ЗАДАЧИ НА ЗАВТРА

### ПРИОРИТЕТ 1: РЕАЛЬНЫЙ AI (КРИТИЧНО!)

#### 1.1 Установить и протестировать Claude CLI
```bash
# Проверить установлен ли
which claude

# Если нет - установить
npm install -g @anthropics/claude-cli
claude auth login
```

#### 1.2 Протестировать интеграцию
- Запустить agent_ran
- Задать вопрос в AI Assistant
- Проверить что вызывается ask_claude_api()
- Убедиться что Claude отвечает умно

#### 1.3 Добавить GitHub Dataset Integration
**Идея:** Загружать ArduPilot Wiki/документацию в контекст Claude

Файл: `core/github_dataset.py`
```python
class GitHubDataset:
    """Load ArduPilot docs from GitHub for AI context"""

    def __init__(self):
        self.docs_repo = "https://github.com/ArduPilot/ardupilot_wiki"
        self.cache_dir = Path.home() / ".mpdiag" / "docs_cache"

    def download_docs(self):
        """Clone/update ArduPilot Wiki"""
        # git clone or git pull

    def search_docs(self, query: str) -> str:
        """Search in downloaded docs"""
        # grep/search in local files

    def get_context_for_error(self, error_type: str) -> str:
        """Get relevant doc section for error"""
        # Return markdown from docs
```

#### 1.4 Улучшить контекст для Claude
- Добавить логи полностью (не только PreArm)
- Добавить параметры дрона
- Добавить последние действия пользователя
- Добавить релевантную документацию из GitHub

---

### ПРИОРИТЕТ 2: АВТОМАТИЗАЦИЯ РЕШЕНИЙ

#### 2.1 Smart Auto-Fix Generator
**Сейчас:** Фиксированный список исправлений
**Нужно:** Claude генерирует исправления на лету

Файл: `core/smart_fixer.py`
```python
class SmartFixer:
    """AI-powered problem solver"""

    def generate_fixes(self, error: str, context: dict) -> List[FixAction]:
        """Ask Claude to generate parameter fixes"""
        prompt = f"""
        Ошибка: {error}
        Контекст: {context}

        Предложи 3-5 исправлений через параметры MAVLink.
        Формат JSON:
        [
            {{
                "title": "...",
                "description": "...",
                "params": {{"PARAM_NAME": value}},
                "severity": "high"
            }}
        ]
        """
        # Call Claude, parse JSON, return FixAction objects
```

#### 2.2 One-Click Complex Fixes
- Последовательность действий (не только параметры)
- Калибровка компаса/акселерометра
- Настройка RC каналов
- Настройка PID с автотюнингом

---

### ПРИОРИТЕТ 3: УЛУЧШЕНИЕ UX

#### 3.1 Индикатор AI Status
Добавить в GUI:
```
[🧠 AI: Claude Connected ✓] [⚡ GitHub Docs: Loaded]
```

#### 3.2 История чата
Сохранять диалог с AI в файл:
```
~/.mpdiag/chat_history/
```

#### 3.3 Progress Bar для AI
Показывать:
```
🧠 Анализирую логи...
🧠 Проверяю документацию...
🧠 Генерирую решение...
```

---

## 📁 ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ

### Основные:
1. **core/unified_agent.py** - уже добавлен ask_claude_api(), нужно тестировать
2. **core/github_dataset.py** - СОЗДАТЬ (новый файл)
3. **core/smart_fixer.py** - СОЗДАТЬ (новый файл)
4. **main.py** - добавить AI status indicator
5. **config.yaml** - добавить:
   ```yaml
   ai:
     enabled: true
     provider: "claude"  # or "ollama", "openai"
     model: "claude-3-sonnet"
     github_docs: true
   ```

---

## 🧪 ТЕСТИРОВАНИЕ

### Сценарии для теста:

1. **Сложный вопрос:**
   ```
   Вопрос: "Дрон дёргается в полёте, что делать?"
   Ожидание: Claude анализирует логи, проверяет PID, предлагает тюнинг
   ```

2. **Неизвестная ошибка:**
   ```
   Вопрос: "Что означает EKF3 variance?"
   Ожидание: Claude ищет в GitHub docs и объясняет
   ```

3. **Комплексное решение:**
   ```
   Вопрос: "Как настроить Auto Tune?"
   Ожидание: Пошаговая инструкция + авто-применение параметров
   ```

---

## 🐛 ИЗВЕСТНЫЕ БАГИ

1. ❌ Pattern matching слишком примитивный
2. ❌ Auto-Fix показывает фиксированный список (нужна генерация AI)
3. ❌ Нет прогресса при долгих операциях
4. ❌ Claude CLI может не быть установлен (нужен fallback на Ollama)

---

## 💡 ИДЕИ НА БУДУЩЕЕ

### v6.2 - RAG Integration
- Загружать всю документацию ArduPilot
- Векторная база знаний (Chroma/FAISS)
- Semantic search по документации

### v6.3 - Telemetry Analysis
- Real-time анализ телеметрии
- Предсказание проблем ДО полёта
- Оптимизация настроек на основе логов

### v6.4 - Multi-Agent System
- Agent 1: Диагностика
- Agent 2: Решение проблем
- Agent 3: Оптимизация настроек
- Agent 4: Обучение пользователя

---

## 📝 ЗАМЕТКИ ДЛЯ CLAUDE AI (ЧИТАЙ ЭТО ПЕРВЫМ ДЕЛОМ!)

**Привет, Claude! Если ты читаешь это завтра утром:**

1. **Главная задача:** Сделать AI Assistant РЕАЛЬНО УМНЫМ
   - Сейчас он тупой (pattern matching)
   - Нужна интеграция с Claude API (ask_claude_api уже добавлен!)
   - Протестируй работу с `claude` CLI

2. **Где код:**
   - `/home/user_1/Desktop/No_problem/Claude/MPDiagnosticAgent/`
   - Основной файл: `core/unified_agent.py`
   - Там уже есть метод `ask_claude_api()` - используй его!

3. **Старый рабочий пример:**
   - `/home/user_1/missionplanner/diagnostic_agent_pro.py`
   - Там есть рабочий `ask_claude()` метод
   - Можешь посмотреть как там сделано

4. **Что проверить:**
   ```bash
   which claude  # Установлен ли Claude CLI?
   agent_ran     # Запускается ли новый агент?
   ```

5. **Тестовый вопрос:**
   ```
   Запусти agent_ran, в AI Assistant напиши:
   "Почему у меня дрон не взлетает? Ошибка RC not found"

   Должен вызваться ask_claude_api() и получить умный ответ!
   ```

6. **Если Claude CLI не работает:**
   - Попробуй Ollama (уже установлен: `/usr/local/bin/ollama`)
   - Или добавь OpenAI API
   - Главное - чтобы был РЕАЛЬНЫЙ AI, а не pattern matching!

---

## 🎯 КРИТЕРИЙ УСПЕХА

**Завтра считается успешным днём если:**

✅ AI Assistant отвечает УМНО на любой вопрос (не шаблонами!)
✅ Claude API работает и интегрирован
✅ Auto-Fix генерируется AI-ом (не фиксированный список)
✅ Есть загрузка документации с GitHub
✅ Пользователь получает реальную помощь, а не "Задайте конкретный вопрос"

---

## 📞 КОНТАКТЫ / ССЫЛКИ

- GitHub Repo: https://github.com/AlexandrTerentev/Claude/tree/main/MPDiagnosticAgent
- ArduPilot Wiki: https://github.com/ArduPilot/ardupilot_wiki
- Claude CLI: https://www.npmjs.com/package/@anthropics/claude-cli
- Ollama: https://ollama.com/

---

**Удачи завтра! 🚀**
