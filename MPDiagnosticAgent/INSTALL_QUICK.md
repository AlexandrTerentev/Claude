# Quick Install Guide - MPDiagnosticAgent v6.0

## Быстрая установка (Linux/Ubuntu)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/AlexandrTerentev/Claude.git
cd Claude/MPDiagnosticAgent

# 2. Установить зависимости
pip3 install -r requirements.txt

# 3. (Опционально) Установить глобальную команду
sudo ./install.sh

# 4. Запустить!
agent_ran
# ИЛИ
python3 main.py
```

## Первый запуск

1. **Подключите дрон** через USB (он появится как `/dev/ttyACM0` или `/dev/ttyUSB0`)

2. **Запустите приложение:**
   ```bash
   python3 main.py
   ```

3. **Вы увидите 4 вкладки:**
   - 💬 **AI Assistant** - чат для вопросов
   - 🔧 **Auto-Fix** - автоматические исправления
   - 📥 **Download Logs** - скачивание логов
   - ⚙️ **Settings** - настройки

4. **Подключитесь к дрону:**
   - В верхней панели выберите порт (автоопределение)
   - Нажмите **Connect**
   - Статус изменится на 🟢 Connected

5. **Используйте AI Assistant:**
   - Вкладка **💬 AI Assistant**
   - Напишите: "анализ" или "показать логи"
   - Задавайте вопросы на русском

6. **Применяйте исправления:**
   - Вкладка **🔧 Auto-Fix**
   - Увидите список проблем
   - Нажмите **Apply Fix** для исправления

## Примеры вопросов AI Assistant

```
анализ
показать логи
почему дрон не взлетает?
что означает "RC not found"?
как исправить?
```

## Установка глобальной команды

После установки через `sudo ./install.sh` можно запускать из любой директории:

```bash
agent_ran              # GUI интерфейс
agent_ran --cli        # CLI режим
agent_ran --version    # Версия
```

## Требования

- Python 3.8+
- pymavlink
- tkinter (обычно уже установлен)
- USB порт для дрона

## Troubleshooting

**"Permission denied" при доступе к порту:**
```bash
sudo usermod -a -G dialout $USER
# Перезайдите в систему
```

**Порт не определяется:**
```bash
ls /dev/ttyACM* /dev/ttyUSB*
# Проверьте, что дрон подключен
```

**GUI не запускается:**
```bash
sudo apt install python3-tk
```

## Ссылки

- 📘 [Полная документация](docs/user_guide.md)
- 🐛 [Troubleshooting](docs/troubleshooting.md)
- 📦 [GitHub Repository](https://github.com/AlexandrTerentev/Claude/tree/main/MPDiagnosticAgent)
