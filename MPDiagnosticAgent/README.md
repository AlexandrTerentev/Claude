# Mission Planner Diagnostic Agent

AI-powered diagnostic assistant для Mission Planner. Помогает диагностировать проблемы с дроном, настраивать параметры и анализировать полетные данные.

## Статус проекта

**Текущая версия:** 1.0.0 (Этап 1 - MVP)

**Реализовано:**
- ✅ Структура проекта
- ✅ Базовый UI (панель чата)
- ✅ Python заглушка для тестирования
- ✅ C# плагин с IronPython интеграцией
- ✅ Build script

**В разработке:**
- 🔄 Анализ логов Mission Planner (Этап 2)
- 🔄 База знаний для диагностики моторов (Этап 3)
- ⏳ Калибровочные гайды (Этап 4)
- ⏳ Доступ к параметрам (Этап 5)
- ⏳ Интеграция Claude API (Этап 6)

---

## Возможности (после завершения MVP)

- **Диагностика проблем:** Анализ логов и поиск причин ошибок
- **Помощь с настройкой:** Пошаговые гайды по калибровке
- **Анализ полетных данных:** Разбор записей полетов
- **Интерактивный чат:** Задавайте вопросы естественным языком

---

## Установка

### Требования

- **Mission Planner** установлен в `/home/user_1/missionplanner/`
- **Mono** (для компиляции C#): `sudo apt-get install mono-devel`
- **Python** не требуется (используется встроенный IronPython)

### Шаги установки

1. **Убедитесь, что проект в правильной папке:**
   ```bash
   ls /home/user_1/Desktop/No_problem/Claude/MPDiagnosticAgent/
   # Должны быть: plugin/, engine/, knowledge/, config/
   ```

2. **Перейдите в папку plugin:**
   ```bash
   cd /home/user_1/Desktop/No_problem/Claude/MPDiagnosticAgent/plugin/
   ```

3. **Запустите build script:**
   ```bash
   ./build.sh
   ```

   Скрипт:
   - Скомпилирует `DiagnosticAgent.cs` в `DiagnosticAgent.dll`
   - Скопирует DLL в `/home/user_1/missionplanner/plugins/`

4. **Перезапустите Mission Planner**

5. **Проверьте установку:**
   - Откройте вкладку **Flight Data**
   - Справа должна появиться панель **Diagnostic Agent**
   - Попробуйте написать сообщение

---

## Использование

### Текущая версия (тестовая)

На данный момент агент просто возвращает эхо ваших сообщений:

```
You: Hello
Agent: Echo: Hello
       Python agent is working!
```

Это тест связи между C# UI и Python движком.

### После завершения MVP

Вы сможете задавать вопросы:

```
You: Why aren't my motors spinning?
Agent: [анализирует логи]

DIAGNOSIS: RC Controller Not Calibrated

CAUSE: Mission Planner doesn't know the range of your RC inputs

SOLUTION:
1. Go to: Initial Setup > Mandatory Hardware > Radio Calibration
2. Turn on your RC transmitter
...
```

---

## Структура проекта

```
MPDiagnosticAgent/
├── plugin/
│   ├── DiagnosticAgent.cs      # C# плагин (UI + IronPython)
│   ├── DiagnosticAgent.dll     # Скомпилированный плагин
│   └── build.sh                # Скрипт компиляции
│
├── engine/
│   ├── agent_core.py           # Главный модуль (✅ готов)
│   ├── log_analyzer.py         # Анализ логов (⏳ следующий)
│   ├── knowledge_base.py       # База знаний (⏳ этап 3)
│   └── claude_api.py           # Claude API (⏳ этап 6)
│
├── knowledge/
│   ├── motor_issues.json       # Проблемы с моторами
│   ├── calibration.json        # Калибровка
│   └── prearm_errors.json      # PreArm ошибки
│
├── config/
│   ├── settings.json           # Настройки агента
│   └── api_keys.json           # API ключи (для этапа 6)
│
└── README.md                    # Этот файл
```

---

## Разработка

### Компиляция вручную

Если `build.sh` не работает:

```bash
cd /home/user_1/Desktop/No_problem/Claude/MPDiagnosticAgent/plugin/

mcs -t:library \
    -r:/home/user_1/missionplanner/MissionPlanner.exe \
    -r:/home/user_1/missionplanner/IronPython.dll \
    -r:/home/user_1/missionplanner/Microsoft.Scripting.dll \
    -r:System.Windows.Forms.dll \
    -r:System.Drawing.dll \
    -r:System.Core.dll \
    DiagnosticAgent.cs \
    -out:DiagnosticAgent.dll

# Копирование
cp DiagnosticAgent.dll /home/user_1/missionplanner/plugins/
```

### Разработка Python модулей

Python код можно редактировать и тестировать без перекомпиляции C#:

1. Отредактируйте `engine/agent_core.py`
2. Перезапустите Mission Planner
3. Изменения применятся автоматически

### Отладка

**Логи Mission Planner:**
```bash
tail -f "/home/user_1/missionplanner/Mission Planner/MissionPlanner.log"
```

**Ошибки плагина:**
- Отображаются в панели чата красным цветом
- Также в логах Mission Planner

---

## Следующие шаги

### Этап 2: Анализ логов (текущий)

Создать `engine/log_analyzer.py`:
- Парсинг логов Mission Planner
- Поиск PreArm ошибок
- Поиск ERROR/CRITICAL сообщений

### Этап 3: База знаний

Создать `engine/knowledge_base.py` и `knowledge/motor_issues.json`:
- Правила диагностики
- Решения типичных проблем
- Пошаговые инструкции

---

## Часто задаваемые вопросы (FAQ)

**Q: Панель не появляется после установки**

A: Проверьте:
1. DLL скопирован в `/home/user_1/missionplanner/plugins/`
2. Mission Planner перезапущен
3. Логи на наличие ошибок: `tail "/home/user_1/missionplanner/Mission Planner/MissionPlanner.log"`

**Q: Ошибка "Project folder not found"**

A: Убедитесь, что проект находится в `/home/user_1/Desktop/No_problem/Claude/MPDiagnosticAgent/`

**Q: Ошибка компиляции**

A: Установите Mono: `sudo apt-get install mono-devel`

**Q: Python agent не работает**

A: Проверьте существование `engine/agent_core.py`

---

## Лицензия

Свободное использование для личных и образовательных целей.

---

## Контрибьюторы

- Claude (AI Assistant)
- User (Developer)

---

## Changelog

### v1.0.0 (2025-12-04)
- Инициализация проекта
- Базовый UI (панель чата)
- Python stub
- Build system
- Этап 1 завершен ✅

---

**Удачи в разработке! 🚁**
