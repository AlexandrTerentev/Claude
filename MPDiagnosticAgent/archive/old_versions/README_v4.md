# Агент Диагностики Mission Planner v4.0
# Mission Planner Diagnostic Agent v4.0

## Что нового / What's New

### Версия 4.0 (Multi-Tab + Russian)
✅ **Полная поддержка русского языка** / Full Russian language support
✅ **Работа на всех вкладках** / Works on all tabs (DATA, SETUP, CONFIG)
✅ **Чистый C#** / Pure C# (no Python dependencies)
✅ **Улучшенная производительность** / Improved performance
✅ **UTF-8 поддержка** / UTF-8 support (русские символы работают идеально)

## Возможности / Features

Агент может помочь с / The agent can help with:
- 🔧 Диагностика моторов (Motor diagnostics)
- 📐 Калибровка датчиков (Sensor calibration)
- 📋 Анализ логов (Log analysis)
- ⚠️ Поиск ошибок PreArm (PreArm error detection)
- 💡 Пошаговые решения (Step-by-step solutions)
- 🗣️ Понимает русский и английский (Understands Russian & English)

## Установка / Installation

### Шаг 1: Скопировать файл / Copy file

```bash
# Скопировать новый плагин в папку plugins
cp /home/user_1/Desktop/No_problem/Claude/MPDiagnosticAgent/plugin/DiagnosticAgent_MultiTab.cs \
   /home/user_1/missionplanner/plugins/
```

### Шаг 2: Опционально - Удалить старые версии / Optional - Remove old versions

```bash
# Если хотите удалить старые версии плагина
cd /home/user_1/missionplanner/plugins/

# Создать резервные копии (на всякий случай)
mv DiagnosticAgent_v2.cs DiagnosticAgent_v2.cs.bak 2>/dev/null
mv DiagnosticAgent_Python.cs DiagnosticAgent_Python.cs.bak 2>/dev/null
```

### Шаг 3: Перезапустить Mission Planner / Restart Mission Planner

```bash
# Закрыть Mission Planner полностью
# Запустить заново
# Подождать 1-2 минуты для компиляции плагина
```

### Шаг 4: Проверить работу / Verify

1. Открыть Mission Planner
2. Перейти на вкладку **DATA** (Flight Data)
3. Справа должна появиться панель **"Агент Диагностики"**
4. Попробовать команду: `помощь` или `help`

## Использование / Usage

### Основные команды / Basic Commands

| Русский | English | Описание / Description |
|---------|---------|------------------------|
| `помощь` | `help` | Показать список команд / Show command list |
| `статус` | `status` | Статус дрона / Drone status |
| `проверить ошибки` | `check errors` | Найти ошибки в логах / Find errors in logs |
| `prearm` | `prearm` | Проверить ошибки PreArm / Check PreArm errors |
| `показать логи` | `show logs` | Последние записи лога / Recent log entries |

### Примеры вопросов / Example Questions

**На русском:**
- "Почему не крутятся моторы?"
- "Как калибровать компас?"
- "Моторы не работают"
- "Проблема с батареей"

**In English:**
- "Why won't my motors spin?"
- "How to calibrate compass?"
- "Motors not working"
- "Battery issue"

### Работа на разных вкладках / Multi-Tab Support

Агент доступен на всех вкладках:
- **DATA** (Flight Data) - основная вкладка полетных данных
- **SETUP** (Initial Setup) - первоначальная настройка
- **CONFIG** (Configuration) - конфигурация параметров

История чата сохраняется при переключении между вкладками!

## Примеры использования / Usage Examples

### Пример 1: Диагностика моторов / Motor Diagnosis

```
Вы: почему не крутятся моторы?

Агент:
ДИАГНОСТИКА МОТОРОВ/АРМИРОВАНИЯ
MOTOR/ARMING DIAGNOSIS:

Найдено PreArm ошибок: 2
Found PreArm errors: 2

✗ RC not calibrated
✗ Compass not calibrated

==================================================
РЕКОМЕНДУЕМЫЕ ДЕЙСТВИЯ (RECOMMENDED ACTIONS):
==================================================

! КОНТРОЛЛЕР RC НЕ ОТКАЛИБРОВАН (RC NOT CALIBRATED)

РЕШЕНИЕ (SOLUTION):
1. Перейти: Initial Setup > Mandatory Hardware > Radio Calibration
2. Включить передатчик RC / Turn on RC transmitter
3. Подвигать все стики / Move all sticks to extremes
4. Нажать 'Calibrate Radio'
5. Проверить ЗЕЛЕНЫЕ полосы / Verify GREEN bars
6. Нажать 'Click when Done'
...
```

### Пример 2: Проверка статуса / Status Check

```
Вы: статус

Агент:
СВОДКА СОСТОЯНИЯ СИСТЕМЫ (SYSTEM STATUS SUMMARY):

Режим (Mode): STABILIZE
Армирован (Armed): False
Высота (Altitude): 0.0m
Напряжение (Voltage): 12.6V
GPS: 3D Fix

✓ ОК - Ошибки не найдены (OK - No errors found)
```

### Пример 3: Калибровка / Calibration

```
Вы: как калибровать компас?

Агент:
РУКОВОДСТВО ПО КАЛИБРОВКЕ (CALIBRATION GUIDE):

КОМПАС (COMPASS):
  Initial Setup > Mandatory Hardware > Compass
  - Нажать 'Onboard Mag Calibration'
  - Выйти на улицу, вращать медленно / Go outside, rotate slowly
...
```

## Технические детали / Technical Details

### Архитектура / Architecture

**Файл:** `DiagnosticAgent_MultiTab.cs` (~900 строк)

**Структура:**
```
├── Strings class (строки 1-180)
│   └── Все локализованные строки / All localized strings
│
├── LogEntry class (строки 181-190)
│   └── Модель записи лога / Log entry model
│
└── DiagnosticAgent class (строки 191-900)
    ├── UI Components (панель чата / chat panel)
    ├── Tab Management (поддержка вкладок / tab support)
    ├── Log Analysis (анализ логов / log analysis)
    └── Diagnostic Logic (логика диагностики / diagnostic logic)
```

### Требования / Requirements

- **Mission Planner** (версия с поддержкой C# плагинов)
- **.NET Framework** (обычно уже установлен с Mission Planner)
- **Файл лога:** `/home/user_1/missionplanner/Mission Planner/MissionPlanner.log`

### Поддерживаемые платформы / Supported Platforms

- ✅ Linux (протестировано / tested)
- ✅ Windows (должно работать / should work)
- ⚠️ macOS (не тестировалось / not tested)

## Устранение неполадок / Troubleshooting

### Проблема: Панель не появляется / Panel doesn't appear

**Решение / Solution:**
1. Проверить логи Mission Planner:
   ```bash
   tail -100 /home/user_1/missionplanner/Mission\ Planner/MissionPlanner.log | grep -i "diagnostic\|plugin\|error"
   ```

2. Искать сообщения:
   - `Plugin Load DiagnosticAgent_MultiTab.cs` ✅
   - `Plugin Init` ✅
   - Если есть ошибки компиляции - проверить синтаксис

3. Подождать 2-3 минуты после запуска Mission Planner

### Проблема: Русские символы не работают / Russian characters broken

**Решение / Solution:**
1. Убедиться что файл .cs сохранен в UTF-8:
   ```bash
   file -i DiagnosticAgent_MultiTab.cs
   # Должно быть: charset=utf-8
   ```

2. Если кодировка неправильная:
   ```bash
   iconv -f ISO-8859-1 -t UTF-8 DiagnosticAgent_MultiTab.cs > temp.cs
   mv temp.cs DiagnosticAgent_MultiTab.cs
   ```

### Проблема: Агент не видит на вкладках SETUP/CONFIG / Agent not visible on SETUP/CONFIG tabs

**Причина:** Mission Planner может использовать другие названия для вкладок

**Решение:**
- Это нормально - агент всегда работает на вкладке DATA
- На других вкладках работа зависит от версии Mission Planner
- Основной функционал доступен на DATA вкладке

### Проблема: "Файл лога не найден" / "Log file not found"

**Решение / Solution:**
1. Проверить путь к логу:
   ```bash
   ls -la "/home/user_1/missionplanner/Mission Planner/MissionPlanner.log"
   ```

2. Если файл в другом месте, отредактировать DiagnosticAgent_MultiTab.cs:
   - Найти строку: `private string logPath = "..."`
   - Изменить путь на правильный
   - Перезапустить Mission Planner

## Сравнение версий / Version Comparison

| Функция / Feature | v2.0 (Basic) | v3.0 (Python) | v4.0 (Multi-Tab) |
|-------------------|--------------|---------------|------------------|
| Русский язык / Russian | ❌ | Частично / Partial | ✅ Полностью / Full |
| Английский / English | ✅ | ✅ | ✅ |
| Вкладки / Tabs | DATA only | DATA only | DATA+SETUP+CONFIG |
| Зависимости / Dependencies | None | IronPython | None |
| Анализ логов / Log analysis | Базовый / Basic | Полный / Full | Полный / Full |
| Диагностика PreArm / PreArm diagnostics | ❌ | ✅ | ✅ |
| Решения проблем / Solutions | ❌ | ✅ | ✅ Локализованные |
| Производительность / Performance | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |

## Удаление / Uninstallation

Если нужно удалить агент / To remove the agent:

```bash
cd /home/user_1/missionplanner/plugins/
rm DiagnosticAgent_MultiTab.cs
# Перезапустить Mission Planner / Restart Mission Planner
```

Для возврата к старой версии / To revert to old version:

```bash
cd /home/user_1/missionplanner/plugins/
mv DiagnosticAgent_v2.cs.bak DiagnosticAgent_v2.cs
# Перезапустить Mission Planner / Restart Mission Planner
```

## Разработка / Development

### Структура проекта / Project Structure

```
MPDiagnosticAgent/
├── plugin/
│   ├── DiagnosticAgent_MultiTab.cs    ← НОВАЯ ВЕРСИЯ / NEW VERSION
│   ├── DiagnosticAgent_Python.cs      (старая / old)
│   └── DiagnosticAgent.cs             (старая / old)
│
├── engine/                             (больше не нужно / no longer needed)
│   ├── agent_core.py
│   └── log_analyzer.py
│
├── knowledge/                          (не используется в v4.0)
│   └── motor_issues.json
│
├── README_v4.md                        ← ЭТО / THIS
└── README.md                           (старая / old)
```

### Модификация / Modification

Для изменения агента / To modify the agent:

1. Редактировать `DiagnosticAgent_MultiTab.cs`
2. Изменить строки в классе `Strings` для новых сообщений
3. Добавить новые методы диагностики в секцию `Diagnostic Methods`
4. Сохранить файл
5. Перезапустить Mission Planner (он автоматически перекомпилирует)

### Добавление новых команд / Adding New Commands

Пример добавления команды "тест" / Example adding "test" command:

```csharp
// В методе ProcessQuery:
if (query.Contains("тест") || query.Contains("test"))
    return RunTest();

// Добавить новый метод:
private string RunTest()
{
    return "Тест пройден успешно!\nTest passed successfully!";
}
```

## Обратная связь / Feedback

Если нашли баг или хотите предложить улучшение:

1. Проверить логи: `/home/user_1/missionplanner/Mission Planner/MissionPlanner.log`
2. Описать проблему с примерами
3. Указать версию Mission Planner

## Лицензия / License

Создано для образовательных целей и личного использования с Mission Planner.
Created for educational purposes and personal use with Mission Planner.

## Авторы / Authors

**Version 4.0:**
- Разработка / Development: Claude (Anthropic)
- Тестирование / Testing: User

**Based on:**
- Mission Planner Plugin API
- ArduPilot diagnostic guidelines

---

## Быстрая шпаргалка / Quick Cheat Sheet

### Команды / Commands
```
помощь / help              - Справка
статус / status            - Статус дрона
проверить ошибки           - Найти ошибки
prearm                     - Проверить PreArm
показать логи / show logs  - Последние логи
```

### Частые вопросы / FAQ

**Q: Можно ли использовать только английский?**
A: Да, все команды работают и на русском, и на английском.

**Q: Нужен ли Python?**
A: Нет, версия 4.0 - чистый C#, Python не требуется.

**Q: Работает ли на Windows?**
A: Должно работать, но тестировалось только на Linux.

**Q: Можно ли добавить свои команды?**
A: Да, отредактируйте DiagnosticAgent_MultiTab.cs и добавьте свою логику.

**Q: Сохраняется ли история чата?**
A: История сохраняется во время сессии, но очищается при перезапуске.

---

**Версия / Version:** 4.0
**Дата / Date:** 2025-12-05
**Статус / Status:** Готов к использованию / Ready to use ✅
